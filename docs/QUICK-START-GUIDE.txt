═══════════════════════════════════════════════════════════════════════════
                        QUICK START GUIDE
          Supabase Auth + Manual Google Sheets Workout Assignment
═══════════════════════════════════════════════════════════════════════════

⏱️  Total Setup Time: 20 minutes
💰 Cost: $0 (completely free)
🎯 Result: Professional authentication + Easy workout management

───────────────────────────────────────────────────────────────────────────
STEP 1: Create Supabase Project (5 minutes)
───────────────────────────────────────────────────────────────────────────

1. Go to https://supabase.com
2. Sign up (free account)
3. Click "New Project"
4. Fill in:
   - Name: viltrum-fitness
   - Database Password: [create a strong password]
   - Region: [choose closest to your users]
5. Wait 2 minutes for project to initialize
6. Go to: Settings → API
7. Copy these two values:
   ✏️ Project URL: https://xxxxx.supabase.co
   ✏️ anon public key: eyJ... (long string)

───────────────────────────────────────────────────────────────────────────
STEP 2: Update Your Code (5 minutes)
───────────────────────────────────────────────────────────────────────────

1. Replace your index.html with: index-with-signup.html
2. Add auth.js to your project folder
3. Open auth.js and update lines 7-8:

   const SUPABASE_URL = 'https://xxxxx.supabase.co';  ← Your URL from Step 1
   const SUPABASE_ANON_KEY = 'eyJ...';                ← Your key from Step 1

4. Add to ALL other pages (dashboard.html, workout.html, nutrition.html):
   
   <head>
     <!-- Your existing head content -->
     <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
     <script src="auth.js"></script>
     <script>
       // Protect this page
       checkAuth();
     </script>
   </head>

───────────────────────────────────────────────────────────────────────────
STEP 3: Update Google Sheets (2 minutes)
───────────────────────────────────────────────────────────────────────────

Open your Google Sheet → "Users" tab

Change Column A header from "utente" to "utente (email)"

Your sheet should look like:

| utente (email)     | scadenza   | workout 1    | workout 2    | workout 3 |
|--------------------|------------|--------------|--------------|-----------|
| john@email.com     | 2025-12-31 | Upper Body A | Lower Body B | Cardio 1  |
| sarah@email.com    | 2026-06-30 | Beginner     | Core Work    |           |

That's it! Your Google Apps Script stays the same.

───────────────────────────────────────────────────────────────────────────
STEP 4: Test It! (5 minutes)
───────────────────────────────────────────────────────────────────────────

TEST A: Create Account & Login
1. Open your website
2. Click "Sign up"
3. Enter:
   - Name: Test User
   - Email: test@youremail.com  ← Use your real email
   - Password: test123
4. Click "Sign Up"
5. Check your email for verification link
6. Click verification link
7. Go back to your site
8. Login with test@youremail.com / test123
9. ✅ Should redirect to dashboard

TEST B: Assign Workouts
1. Open Google Sheets → Users tab
2. Add new row:
   test@youremail.com | 2025-12-31 | Workout Name 1 | Workout Name 2
3. Refresh your website
4. ✅ Should see assigned workouts!

TEST C: Expiration Check
1. In Google Sheets, set expiration to past date: 2024-01-01
2. Refresh website
3. ✅ Should show "Subscription Expired"

───────────────────────────────────────────────────────────────────────────
STEP 5: Your Daily Workflow
───────────────────────────────────────────────────────────────────────────

When New Client Signs Up:
1. You'll see them in: Supabase Dashboard → Authentication → Users
2. Note their email: newclient@email.com
3. Open Google Sheets
4. Add row: newclient@email.com | 2026-12-31 | Beginner | Core
5. Done! They can now login and see workouts

When Changing Client's Workouts:
1. Open Google Sheets
2. Find their row
3. Edit workout columns
4. Changes appear immediately (no redeployment!)

When Subscription Expires:
1. Open Google Sheets
2. Update their expiration date
3. Or delete their row to remove access

───────────────────────────────────────────────────────────────────────────
FILES YOU NEED
───────────────────────────────────────────────────────────────────────────

Your Project Structure:

viltrum-fitness-V4/
├── index.html          ← Replace with index-with-signup.html
├── dashboard.html      ← Add auth.js script tag
├── workout.html        ← Add auth.js script tag
├── nutrition.html      ← Add auth.js script tag
├── auth.js             ← NEW FILE (authentication)
├── script.js           ← Keep existing (workout loading)
└── styles.css          ← Keep existing

───────────────────────────────────────────────────────────────────────────
TROUBLESHOOTING
───────────────────────────────────────────────────────────────────────────

❌ "Can't sign up"
→ Check: SUPABASE_URL and SUPABASE_ANON_KEY are correct in auth.js
→ Check: Email confirmation is enabled (Supabase → Authentication → Email Templates)

❌ "Login works but can't see workouts"
→ Check: Email in Google Sheets matches exactly (lowercase)
→ Check: Google Apps Script URL is correct in script.js
→ Check: Expiration date is in the future

❌ "Redirects to login immediately after login"
→ Check: All pages have <script src="auth.js"></script>
→ Check: checkAuth() is called on protected pages

❌ "User not receiving verification email"
→ Check spam folder
→ Or disable email confirmation: Supabase → Authentication → Providers → Email → Disable "Confirm email"

───────────────────────────────────────────────────────────────────────────
WHAT'S NEXT?
───────────────────────────────────────────────────────────────────────────

After you're comfortable with manual assignment:

📈 UPGRADE TO AUTOMATIC (Optional - Week 2+)
   → Use SETUP-INSTRUCTIONS-AUTO-ADD.txt
   → New users automatically get default workouts
   → You can still customize in Google Sheets

🎨 CUSTOMIZE THE LOGIN PAGE
   → Change colors in index.html <style>
   → Add your logo
   → Match your brand

📧 ADD EMAIL NOTIFICATIONS
   → Get notified when users sign up
   → Send welcome emails
   → Use notification-system.js

───────────────────────────────────────────────────────────────────────────
SUPPORT
───────────────────────────────────────────────────────────────────────────

Supabase Issues: https://supabase.com/docs
Google Sheets API: Your existing Google Apps Script
Authentication Help: Check auth.js comments

───────────────────────────────────────────────────────────────────────────

You're done! 🎉

Users can now:
✅ Create accounts securely
✅ Login with email/password
✅ See their assigned workouts
✅ Get automatic session management

You can:
✅ Manage all workouts in Google Sheets (easy!)
✅ Set expiration dates
✅ Add/remove users instantly
✅ No more plain-text passwords!

═══════════════════════════════════════════════════════════════════════════
